package jmat.com.jumped.Weapons;

import android.graphics.drawable.BitmapDrawable;

public class Gun extends Weapon {
    public Gun(int damagePerHit, BitmapDrawable skin, int range) {
        super(damagePerHit, skin, range);
    }
}
